// Analogy data for problems 1-50 (keyed by approach name)
module.exports = {
  1: { "Hash set": "Like a bouncer's guest list — the moment someone walks in twice, you know there's a duplicate." },
  2: { "Frequency count": "Like comparing two Scrabble racks — if every letter count matches, the words are anagrams." },
  3: { "Hash map complement": "Like finding the missing puzzle piece — for each piece, check if its partner is already on the table." },
  4: {
    "Sorted string key": "Like sorting each word's letters into a canonical form — anagrams collapse to the same fingerprint.",
    "Frequency count key": "Like tallying ingredients in recipe cards — same letter counts mean the same dish (anagram group).",
  },
  5: {
    "Min-heap of size k": "Like keeping only the k tallest people in line — evict the shortest whenever someone taller shows up.",
    "Bucket sort by frequency": "Like sorting mail into pigeonholes by frequency, then grab from the fullest slots first.",
  },
  6: { "Prefix + suffix products": "Like each person in a line multiplying everyone else — track left-side and right-side totals separately." },
  7: { "Length-delimited encoding": "Like shipping boxes labeled #5#apple — the size tag tells you exactly where each item starts and ends." },
  8: { "Hash set streak expansion": "Like spotting domino chains — only start counting from the first tile of each consecutive run." },
  9: { "Hash sets per row/col/box": "Like three bouncers at a Sudoku party — each row, column, and box keeps its own 'already seen' list." },
  10: { "Two pointers inward": "Like reading a palindrome from both ends — skip junk, compare inward until they meet or mismatch." },
  11: { "Sorted two pointers": "Like two shoppers on a sorted price list — one at the cheap end, one at the expensive, meeting in the middle." },
  12: { "Sort + two pointers": "Like fixing one person in a sorted line and having two friends walk inward to find a matching pair for zero sum." },
  13: { "Greedy two pointers": "Like two walls closing in — always move the shorter wall inward, since the taller one still has hope." },
  14: { "Two pointers with max heights": "Like two people at opposite ends of a valley — each tracks the tallest wall they've seen to estimate trapped water." },
  15: { "Track min price + max profit": "Like buying at the lowest price seen so far and selling at today's price for the best profit." },
  16: { "Sliding window + last seen map": "Like a moving spotlight on a stage — expand right, but jump left past the last repeat of any character." },
  17: {
    "Max window with at most k replacements": "Like a spell-checker with k free edits — stretch the window as long as (length − most common char) ≤ k.",
    "Max window with frequency map": "Like a spell-checker with k free edits — track letter counts and stretch while replacements stay within budget.",
  },
  18: { "Fixed window frequency match": "Like sliding a fixed-size stencil over text — check if the letter counts inside match the target pattern." },
  19: { "Expand/shrink with need counts": "Like covering all required toppings on a pizza — grow the slice until every topping appears, then trim the excess." },
  20: { "Monotonic deque": "Like a line of people where shorter ones in back get kicked out — only candidates who might be tallest in the window stay." },
  21: { "Stack matching": "Like nesting boxes — push opens, pop closes; a mismatch or leftover open means invalid nesting." },
  22: {
    "Two stacks": "Like a regular stack plus a shadow stack that mirrors the minimum — every push updates both, every pop removes from both.",
    "Single stack with pairs": "Like storing (value, currentMin) pairs on one stack — each entry remembers the minimum at that depth.",
  },
  23: { "Stack evaluation": "Like a calculator that waits for numbers — operators pop the last two operands, compute, and push the result back." },
  24: { "Monotonic decreasing stack": "Like waiting for warmer weather — each day asks the stack of cooler days 'how long until someone warmer shows up?'" },
  25: { "Sort by position + stack merge": "Like cars merging on a highway — faster cars behind catch up and form fleets; count the surviving groups." },
  26: { "Monotonic increasing stack": "Like building a fence — each bar extends left until a taller bar stops it, giving the width for max rectangle area." },
  27: { "Classic lo/hi binary search": "Like guessing a number in 'hot/cold' — halve the search range each guess until you hit the target." },
  28: { "Treat as 1D sorted array": "Like unrolling a newspaper into one long sorted strip — one binary search covers the whole matrix." },
  29: { "Binary search on eating speed": "Like tuning a machine's speed — too slow misses the deadline, too fast wastes power; binary search finds the minimum that works." },
  30: { "Compare mid with right": "Like finding the lowest point on a rotated hill — compare the middle to the right end to know which half contains the minimum." },
  31: { "Identify sorted half": "Like searching a rotated bookshelf — one half is always properly sorted, so pick that half if the target fits." },
  32: { "Hash map + binary search timestamps": "Like a library catalog with dated editions — find the key, then binary search timestamps for the latest ≤ query time." },
  33: { "Binary search partition": "Like splitting two sorted decks at a cut point — the cut defines left/right halves whose medians meet in the middle." },
  34: { "Iterative reversal": "Like flipping a chain of paper clips — three pointers walk the list, reversing one link at a time." },
  35: { "Dummy head merge": "Like weaving two sorted queues into one line — always attach the smaller front node and advance that list." },
  36: { "Find mid + reverse + merge": "Like folding a list in half — find the middle, reverse the back half, then zip the two halves together." },
  37: { "Fast/slow two pointers": "Like a tortoise and hare — when the fast pointer is n steps ahead, slow is at the node to remove." },
  38: {
    "Hash map old to new": "Like photocopying a scrapbook with sticky notes — map each original page to its clone, then wire up the copies.",
    "Interweave + split": "Like weaving duplicate pages between originals, then splitting the interleaved chain into a clean copy.",
  },
  39: { "Simulate addition with carry": "Like grade-school addition on paper — add digits, carry the overflow, build the result list." },
  40: { "Floyd cycle detection": "Like two runners on a track — if the fast one laps the slow one, there's a loop." },
  41: { "Floyd cycle on index graph": "Like following treasure-map hops — treat indices as pointers; a cycle reveals the duplicate number." },
  42: { "Hash map + doubly linked list": "Like a VIP lounge with a guest list — hash map for instant lookup, linked list for eviction order." },
  43: {
    "Min-heap of k heads": "Like merging k sorted playlists — always pick the smallest current song from the heap of k front tracks.",
    "Divide and conquer merge": "Like a tournament bracket — merge pairs of lists repeatedly until one sorted list remains.",
  },
  44: { "Iterative k-group reversal": "Like flipping cards in batches of k — reverse each group in place, then stitch the batches together." },
  45: { "Recursive DFS swap": "Like flipping a family tree photo — swap left and right children at every node, top to bottom." },
  46: { "Recursive max depth": "Like measuring a tree from root to deepest leaf — depth is 1 plus the taller of the two subtrees." },
  47: { "DFS height with global max": "Like measuring each node's reach — the diameter through a node is left height + right height." },
  48: { "Post-order height check": "Like checking a mobile's balance — if any side hangs too low, the whole structure is unbalanced." },
  49: { "Recursive compare": "Like diffing two family trees — same shape, same values at every node, or they're different." },
  50: { "DFS match at each node": "Like searching for a smaller photo inside a larger one — try matching at every possible starting node." },
};
