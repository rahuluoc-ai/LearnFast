// Analogy data for problems 101-150 (keyed by approach name)
module.exports = {
  101: { "Rob/skip DP": "Like robbing houses on a street — take this house plus best from two doors back, or skip and keep the previous best." },
  102: { "Two linear passes skip ends": "Like robbing a circular street — run the robber twice, skipping either the first or last house." },
  103: { "Expand around center": "Like checking a palindrome by growing outward from each center — odd and even centers both get a try." },
  104: { "Expand around center": "Like counting palindromes by expanding from every center — each match adds one to the count." },
  105: { "Linear DP ways[i]": "Like decoding a phone keypad — each position adds ways from a single digit or a valid two-digit chunk." },
  106: { "Unbounded knapsack DP": "Like making change with unlimited coins — for each amount, try every coin and keep the minimum count." },
  107: { "Track max and min product": "Like tracking best and worst streaks — a negative number flips min into max, so track both." },
  108: { "DP reachable[i]": "Like breaking a sentence into dictionary words — if you can reach position i and the next word is valid, mark i+len reachable." },
  109: { "Patience sorting / tails": "Like the card game Patience — each new card replaces the smallest pile top it can beat, and pile count is LIS length." },
  110: { "0/1 knapsack DP": "Like packing a backpack to exactly half capacity — each number is used once to hit the target sum." },
  111: {
    "Grid DP bottom-up": "Like walking a grid only right or down — each cell's paths = paths from above + paths from the left.",
    "Combinatorics C(m+n-2,m-1)": "Like choosing which steps are 'down' in a fixed walk — pure math counts paths without a grid.",
  },
  112: {
    "2D DP table": "Like aligning two strings on graph paper — match extends the diagonal, mismatch takes the best of up or left.",
    "Space-optimized 1D": "Like aligning two strings with one row of sticky notes — same logic, less memory.",
  },
  113: { "State machine DP (hold/sold/rest)": "Like stock trading with a cooldown — three moods (holding, just sold, resting) govern each day's best profit." },
  114: { "Unbounded knapsack ways": "Like counting coin combinations — each coin can be reused, and ways build up amount by amount." },
  115: { "Subset sum DP": "Like assigning +/- signs — count subsets that sum to half of (total + target)." },
  116: {
    "2D DP match s1/s2": "Like weaving two threads into a third — each char of s3 must come from the next char of s1 or s2.",
    "Space-optimized 1D": "Like weaving two threads with one row of notes — same interleaving check, compressed memory.",
  },
  117: { "DFS + memoization": "Like hiking only uphill — memoize the longest trail from each cell to avoid recomputing paths." },
  118: {
    "2D DP count matches": "Like counting ways to spell t from s — each char match adds paths from the shorter prefixes.",
    "Space-optimized 1D": "Like counting subsequence matches with one row — update right-to-left for each char of s.",
  },
  119: {
    "Levenshtein DP": "Like spell-check distance — insert, delete, or replace to transform one word into another, minimizing edits.",
    "Space-optimized 1D": "Like spell-check with one row of costs — same edit distance, rolling memory.",
  },
  120: { "Interval DP": "Like bursting balloons for max coins — try each balloon as the last to pop in an interval, combining sub-results." },
  121: { "2D DP with .* cases": "Like regex matching with wildcards — '*' means zero or more of the prior char, '.' matches anything." },
  122: { "Kadane's algorithm": "Like finding the best streak — restart the sum when it goes negative, track the all-time maximum." },
  123: {
    "Track farthest reachable": "Like jumping on stones — track the farthest landing spot; if you can't reach index i, you're stuck.",
    "Backward greedy": "Like working backward from the finish — move the goal left whenever a stone can reach it.",
  },
  124: { "Greedy BFS-like jumps": "Like minimum hops in a game — each jump covers a range; count jumps when you must extend the range." },
  125: { "Single pass total + start": "Like driving a circular gas route — if you run empty, restart from the next station; if total gas ≥ total cost, a solution exists." },
  126: { "Sort + hash map groups": "Like dealing consecutive card runs — sort first, then peel off groupSize consecutive cards from each rank." },
  127: { "Track max of needed dims": "Like collecting upgrade tokens — ignore any triplet that overshoots target; win when all three dimensions are covered." },
  128: { "Last occurrence greedy partition": "Like cutting a string so each letter stays in one piece — extend each part to the last appearance of its letters." },
  129: {
    "Two-pass open/close balance": "Like checking parentheses with wildcards — scan forward treating '*' as '(', then backward treating '*' as ')'." ,
    "Greedy range lo/hi": "Like tracking how many open parens '*' could represent — keep a min/max balance range alive after each char.",
  },
  130: { "Merge in one pass": "Like inserting a new meeting into a calendar — merge overlapping blocks in one sweep." },
  131: { "Sort by start + merge": "Like merging overlapping appointments — sort by start, extend the last block or start a new one." },
  132: { "Sort by end + greedy keep": "Like picking non-overlapping meetings — always keep the one that ends earliest to leave room for more." },
  133: { "Sort + check overlap": "Like checking a calendar — sort meetings and flag if any start comes before the previous end." },
  134: {
    "Min-heap of end times": "Like assigning conference rooms — reuse a room if the next meeting starts after the earliest ending one.",
    "Sweep line": "Like counting people entering/leaving — track concurrent events to find peak overlap.",
  },
  135: { "Sort queries + sweep with heap": "Like answering 'smallest fitting box' questions — sweep intervals in, heap by size, pop ones too small." },
  136: {
    "Layer-by-layer rotate": "Like rotating an onion ring by ring — swap four corners at each layer position.",
    "Transpose + reverse rows": "Like flipping a matrix over its diagonal then mirroring each row — equals a 90° clockwise turn.",
  },
  137: { "Boundary simulation": "Like peeling an onion — walk right, down, left, up, shrinking the boundary each lap." },
  138: { "First row/col as markers": "Like using the edges as sticky notes — mark which rows/columns need zeroing without extra space." },
  139: { "Floyd cycle on digit squares": "Like happy-number chain — if slow and fast meet at 1, it's happy; any other cycle means sad." },
  140: { "Simulate carry from end": "Like odometer rollover — add 1 from the right, carry left through 9s, prepend 1 if needed." },
  141: { "Fast exponentiation": "Like repeated squaring — halve the exponent each step, multiply extra when the exponent is odd." },
  142: { "Grade-school multiplication": "Like multiplying on paper — each digit pair contributes to the result array with carry." },
  143: { "Hash map point counts": "Like finding squares from two corners — given two points, check if the other two corners exist in the map." },
  144: { "XOR all elements": "Like canceling pairs in a pile — XOR makes duplicates vanish, leaving the lone number." },
  145: {
    "n & (n-1) loop": "Like peeling one layer off an onion — each n & (n−1) removes the lowest set bit until nothing remains.",
    "Built-in bit count": "Like using a popcount instruction — let the hardware count set bits directly.",
  },
  146: { "DP bits[i] = bits[i>>1] + i&1": "Like inheriting your parent's bit count — half the number's bits plus the last bit." },
  147: { "Bit-by-bit reverse": "Like reading a binary number backwards — shift result left, grab the last bit of n, repeat 32 times." },
  148: {
    "XOR index and values": "Like canceling every number with its index — the one that survives is the missing value.",
    "Sum formula": "Like expected attendance minus actual headcount — the difference is who didn't show up.",
  },
  149: { "XOR + carry loop": "Like adding in binary without '+' — XOR gives sum bits, AND-shift gives carry; repeat until carry is zero." },
  150: { "Extract digits with overflow check": "Like reversing an odometer with guardrails — pop digits from the right, stop before 32-bit overflow." },
};
